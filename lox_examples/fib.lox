fun fib(n) {
    if (n <= 1) return 1;
    var ret = fib(n-1) + fib(n-2);
    return ret;
}

var start = clock();

for (var i = 0; i < 12; i = i + 1) {
    print fib(i);
}

var end = clock();

print "Time to compute (s):";
print end - start;
